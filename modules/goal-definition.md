# Goal Definition Module

Extract and validate the business objectives.

## Rules
1. Reject ambiguous goals (e.g. "Get big", "Grow fast") and raise Gate 1 error.
2. Structure outcome parameters into `schemas/goal.json`.
3. Map timeframe constraints exactly.
