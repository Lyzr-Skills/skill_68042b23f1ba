# Growth Math Module

Deterministic model calculator.

## Execution
- Accepts `schemas/goal.json` and `schemas/baseline.json`.
- Uses programmatic calculations to fill `schemas/growth-model.json`.
- Feeds required numbers back to pipeline.
