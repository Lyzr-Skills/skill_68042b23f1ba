# Hallucination Tests

Detect AI hallucination of parameters, benchmarks, or capabilities.

## Check Rules
1. Assert no invented conversion rates or historical numbers are listed as "Actual".
2. Assert every benchmark listed maps to an identified entry in `schemas/assumption.json`.
3. Assert that team capacity constraints are strictly respected.
