# Configuration Guide

Customize weightings and execution thresholds.

## Custom Weights
- **Channel weights**: Modify `config/channel-scoring.yaml` to adjust cost, speed, or team multipliers.
- **Experiment formula**: Configure ICE/Effort weight parameters in `config/experiment-scoring.yaml`.
