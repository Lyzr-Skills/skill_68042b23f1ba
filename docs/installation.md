# Installation Guide

Install the GTM Strategy Skill into the GTMOS agent workspace.

## Setup Steps
1. Place folders `core/`, `schemas/`, `config/`, `modules/`, `prompts/`, and `tests/` into your skill root directory.
2. Ensure you have a YAML parser and a JSON Schema validation library active.
3. Validate defaults configuration by parsing `config/defaults.yaml`.
